<h2>Your Students:</h2>
<div class="list-group">
<ul class="list-group list-group-horizontal">
{{#each students as |student|}}
  <li class="list-group-item"><a href="/students/{{student.id}}" class="list-group-item list-group-item-action">{{student.name}}</a></li>
  <li class="list-group-item">
  {{#if equipment.length}}
  ✅
  {{/if}}
  </li>
{{/each}}
</ul>
</div>